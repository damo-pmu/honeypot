[honeypot]
# SSH - mapped to host port via SSH_PORT env var
ssh_port = 2222
# Telnet - mapped to host port via TELNET_PORT env var
telnet_port = 23

# Logging - MUST match Docker volume path (Cowrie official uses /cowrie/var/log)
log_path = /cowrie/var/log
log_json = true